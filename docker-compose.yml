services:
  pioneer-remote:
    build: .
    image: pioneer-remote:latest
    container_name: pioneer-remote
    restart: unless-stopped
    ports:
      - "8088:8088"
    environment:
      # Pioneer receiver
      - RECEIVER_HOST=192.168.1.100
      - RECEIVER_PORT=23
      # MQTT / Home Assistant (optional — leave MQTT_HOST blank to disable)
      - MQTT_HOST=
      - MQTT_PORT=1883
      - MQTT_USER=
      - MQTT_PASS=
      - PORT=8088
    network_mode: bridge
